import FetchContainer from '~/components/strateg/fetch-container';
import { VStack } from '~panda/jsx';

const Page = () => {
  return (
    <VStack>
      <FetchContainer />
    </VStack>
  );
};

export default Page;
