{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "panda codegen",
    "dev": "rm -rf .next && next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "setup": "rm -rf .next && rm -rf node_modules && bun install && bun panda codegen --clean"
  },
  "dependencies": {
    "@ark-ui/react": "^2.2.2",
    "next": "^14.2.2",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@pandacss/dev": "^0.37.2",
    "@park-ui/panda-preset": "^0.37.0",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.1.0",
    "typescript": "^5"
  }
}